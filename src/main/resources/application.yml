spring:
  application:
    name: CONFIG-SERVER

    
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dheeraj198922/openeye_config_server
          clone-on-start: true
          
    
  profiles:
    active:
    - dev
server:
  port: 8888